import styled from '@emotion/styled';

export const StyledAppName = styled.div`
    font-family: righteous;
    font-size: 2.5em;
    & > span:first-of-type {
        color: #ffffff;
    }
    & > span:last-of-type {
        color: #606060;
    }
`;
